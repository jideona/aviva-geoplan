// Superseded — split into ProjectListScreen.tsx (project picker) and
// DashboardScreen.tsx (capture tiles + Recent Captures) per the Claude
// Design redesign ("Field Survey App Redesign.dc.html"). Not imported by
// App.tsx anymore. Left in place rather than deleted because this sandbox
// can't remove files from the mounted project folder — safe to delete by
// hand (`rm mobile/src/screens/HomeScreen.tsx`) next time you're on the repo.
export {};
