fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
GeoAlchemy2==0.16.0
psycopg2-binary==2.9.10
alembic==1.14.0
PyJWT==2.10.1
argon2-cffi==23.1.0
python-multipart==0.0.20
shapely==2.0.6
pyproj==3.7.0
fiona==1.10.1
redis==5.2.1
minio==7.2.10
openpyxl==3.1.5
# EXIF DateTime extraction for app/import_existing_network_survey.py (the
# photos' GPS EXIF tags are present but empty — see that script's docstring —
# so only the capture timestamp is usable, not location).
Pillow==11.0.0
svglib==1.5.1
reportlab==4.2.5
# reportlab's renderPM (used to rasterise diagrams to PNG/PDF, both for the
# topology.png/schematic.png endpoints and the Word report) needs one of
# these backends to actually draw — without it, drawToString fails with
# "cannot import desired renderPM backend rlPyCairo" (a pre-existing gap:
# PNG/PDF diagram export never worked in this image before this was added;
# SVG export was unaffected since it skips renderPM entirely).
pycairo==1.29.0
rlPyCairo==0.4.0
python-docx==1.1.2
structlog==24.4.0
pytest==8.3.4
httpx==0.28.1
